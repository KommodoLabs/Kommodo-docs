---
sidebar_position: 2
---

# KommodoFactory

Deploys individual `Kommodo` lending pools for each `(tokenA, tokenB, poolFee)`. Each pool maps to a corresponding Uniswap V3 pool. The factory stores pool addresses and can be queried for existing deployments.

